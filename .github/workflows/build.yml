name: Build

on: push

permissions: write-all

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target:
          - bun-linux-x64
          - bun-linux-arm64
          - bun-darwin-x64
          - bun-darwin-arm64
          - bun-windows-x64
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 1

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - run: bun install --frozen-lockfile

      - run: bun build --compile --target=${{ matrix.target }} ./bin/yate.js --outfile yate

      - name: Upload Artifacts
        uses: actions/upload-artifact@v5
        with:
          name: yate-${{ matrix.target }}${{ contains(matrix.target, 'windows') && '.exe' || '' }}
          path: yate${{ contains(matrix.target, 'windows') && '.exe' || '' }}
          if-no-files-found: error

      - name: Upload Release
        if: startsWith(github.ref, 'refs/tags')
        uses: softprops/action-gh-release@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          files: yate${{ contains(matrix.target, 'windows') && '.exe' || '' }}
